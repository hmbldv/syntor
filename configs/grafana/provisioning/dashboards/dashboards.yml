apiVersion: 1

providers:
  - name: 'SYNTOR Dashboards'
    orgId: 1
    folder: 'SYNTOR'
    folderUid: syntor
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
